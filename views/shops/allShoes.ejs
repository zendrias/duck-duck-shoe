<%- include('../partials/html-head') %>
  <% if (user) { %>
    <%- include('../partials/dashboardNav') %>
      <% } else { %>
        <%- include('../partials/nav') %>
          <% } %>

            <body>
              <h1>All Shoes Here</h1>
              <% shoes.forEach(shoe=>{%>
                <div class="product-cards">
                  <img src="<%= shoe.img %>" alt="Listing Image">
                  <p>
                    <%= shoe.name %>
                  </p>
                  <p>
                    <%= shoe.price %>
                  </p>
                </div>
                <% }) %>
            </body>

            </html>